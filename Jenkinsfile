pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                // Checkout your source code from version control
                //git clone 'github.com/chaudharims/nuget.git'
                echo "hello"
                sh ls
            }
        }     
    }
}
